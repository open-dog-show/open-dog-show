// SPDX-FileCopyrightText: 2026 the OpenDogShow contributors
// SPDX-License-Identifier: AGPL-3.0-only

/**
 * Public surface of the {{dashCase name}} context (ADR-0028): command/response
 * types, handler types, expected-outcome errors, event classes + `_TYPE`
 * constants + payload types, and the `create{{pascalCase name}}Context` wiring
 * factory. Aggregates, repositories, ports, ORM adapters and fakes stay
 * internal — another context or `apps/` may import only what is exported
 * here; tests import internals by deep path.
 */
export { create{{pascalCase name}}Context } from './infrastructure/di/create-{{dashCase name}}-context.js';
export type {
    {{pascalCase name}}ContextDependencies,
    {{pascalCase name}}Context,
} from './infrastructure/di/create-{{dashCase name}}-context.js';
// plop:exports
