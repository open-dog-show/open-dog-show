private stage{{pascalCase (plural name)}}() {
    return this.{{plural (camelCase name)}}.map(({{camelCase name}}) =>
        {{pascalCase name}}.rehydrate({ id: {{camelCase name}}.id, name: {{camelCase name}}.name }),
    );
}
