export const {{plural (camelCase name)}}Table = schema.table('{{plural (snakeCase name)}}', {
    id: uuid('id').primaryKey(),
    clubId: uuid('club_id').notNull(),
    principalId: uuid('user_id').notNull(),
    {{camelCase parent}}Id: uuid('{{snakeCase parent}}_id').notNull(),
    name: text('name').notNull(),
});
